apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "trading.fullname" . }}-shared
  namespace: {{ include "trading.namespace" . }}
  labels:
    {{- include "trading.labels" . | nindent 4 }}
data:
{{- (.Files.Glob "config-embed/*").AsConfig | nindent 2 }}
